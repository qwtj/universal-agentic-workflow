// Placeholder — launches uwf-interviewer agent with gap context (future milestone)
export class InterviewerBridge {
  static launch(_workspaceRoot: string, _gapIds: number[]): void {
    // TODO: invoke uwf-interviewer via VS Code terminal API
  }
}
